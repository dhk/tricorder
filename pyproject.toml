[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "tricorder"
description = "Repository learning system for review patterns, team knowledge, and improvement planning"
requires-python = ">=3.9"
dynamic = ["version"]
dependencies = [
    "anthropic",
    "requests",
    "pyyaml",
]

[project.scripts]
tricorder = "tricorder.cli:main"

[tool.setuptools.dynamic]
version = {file = "VERSION"}

[tool.setuptools.packages.find]
where = ["."]
include = ["tricorder*"]
